
/**
 * 截取格式化时间
 * '2020-03-08 21:45:48' -> '20-03-08'
 */
var subFormatTime = function (formatTime) {
  if (typeof formatTime !== 'string') return formatTime

  return formatTime.substring(2, 10)
}

module.exports = {
  subFormatTime: subFormatTime
}

